type Repo = {
	clone_url: string,
	git_url: string,
	html_url: string,
	ssh_url: string,
	name: string,
	pushed_at: string,
	url: string,
	watchers: number,
	watchers_count: number
}

type Owner = {
	avatar_url:	string,
	html_url: string,
	login: string
}